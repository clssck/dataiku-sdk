name: Release to npm

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version bump type"
        required: true
        default: "patch"
        type: choice
        options:
          - patch
          - minor
          - major

permissions:
  contents: read

concurrency:
  group: release-main
  cancel-in-progress: false

jobs:
  prepare:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          ref: ${{ github.sha }}
          persist-credentials: false

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: "24"

      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: 1.4.0

      - name: Reject stale or non-main dispatches
        run: |
          test "$GITHUB_REF" = refs/heads/main
          remote="$(git ls-remote origin refs/heads/main)"
          test "${remote%%[[:space:]]*}" = "$GITHUB_SHA"

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Prepare versioned candidate once
        env:
          BUMP: ${{ inputs.version }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          bun scripts/release-candidate.mjs prepare "$BUMP" "$RUNNER_TEMP/candidate"

      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: release-candidate-${{ github.run_id }}
          path: ${{ runner.temp }}/candidate/
          if-no-files-found: error
          retention-days: 30

  platform:
    needs: prepare
    uses: ./.github/workflows/ci.yml
    with:
      candidate-artifact: release-candidate-${{ github.run_id }}

  release:
    needs: platform
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: write
      actions: read
      id-token: write
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          ref: ${{ github.sha }}
          persist-credentials: false

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: "24"

      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: 1.4.0

      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: release-candidate-${{ github.run_id }}
          path: ${{ runner.temp }}/candidate

      - name: Restore verified candidate source
        run: bun scripts/release-candidate.mjs restore "$RUNNER_TEMP/candidate"

      - name: Require unchanged main before publication
        run: |
          base="$(bun -p 'require(process.env.RUNNER_TEMP + "/candidate/manifest.json").baseRevision')"
          commit="$(git rev-parse HEAD)"
          remote="$(git ls-remote origin refs/heads/main)"
          remote="${remote%%[[:space:]]*}"
          test "$remote" = "$base" || test "$remote" = "$commit"

      - name: Check publication history
        id: history
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # A failed/ambiguous publish is never automatically retried on a job rerun.
          history="$(gh api --paginate "repos/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID/jobs?filter=all&per_page=100" --jq '[.jobs[] | select(.name == "release") | .steps[]? | select(.name == "Publish candidate to npm" and (.status == "in_progress" or .status == "completed") and .conclusion != "skipped")] | length')"
          if [[ "$history" =~ [1-9] ]]; then
            echo 'publish=false' >> "$GITHUB_OUTPUT"
          else
            echo 'publish=true' >> "$GITHUB_OUTPUT"
          fi

      - name: Publish candidate to npm
        if: steps.history.outputs.publish == 'true'
        run: npm publish "$RUNNER_TEMP/candidate/package.tgz" --ignore-scripts --provenance --access public
        env:
          NODE_AUTH_TOKEN: ""

      - name: Verify npm metadata and canonical tarball availability
        run: bun scripts/release-candidate.mjs verify "$RUNNER_TEMP/candidate"

      - name: Push candidate commit and tag atomically
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          version="v$(bun -p 'require("./package.json").version')"
          git -c credential.helper= -c 'credential.helper=!gh auth git-credential' push --atomic origin HEAD:refs/heads/main "refs/tags/$version:refs/tags/$version"

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          version="v$(bun -p 'require("./package.json").version')"
          if ! gh release view "$version" >/dev/null 2>&1; then
            gh release create "$version" --title "$version" --generate-notes --verify-tag
          fi
